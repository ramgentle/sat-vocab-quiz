services:
  - type: web
    name: sat-vocab-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: CLIENT_URL
        sync: false
      - key: SESSION_SECRET
        generateValue: true
